# required for all
    APPS_LIST=(
        "wget"
        "curl"
        "git"
        "flatpak"
    )

    # required for shell
    APPS_LIST+=(
        "zsh"
        "tar"
        "bat"
        "tree"
        #"multitail" -- via repo utilities
        "fastfetch"
        "unzip"
        "fontconfig"
        "mlocate"
        "htop"
        "stow"
        "wine"
    )

    # required for network
    APPS_LIST+=(
        "autofs"
        "net-tools-deprecated"
    )

    # required for Mozilla
    APPS_LIST+=(
        "MozillaFirefox"
        "MozillaThunderbird"
    )

    # required for fonts
    APPS_LIST+=(
        #"fetchmsttfonts" -- onbekend
        #"fira-code-fonts" -- onbekend
    )

    # required for micro
    APPS_LIST+=(
        "micro-editor"
    )

    # required for desktop
    APPS_LIST+=(
        #"plank" -- onbekend
        "yakuake"
        "plasma-vault"
     #   "plasma5-firewall" -- niet nodig
    )

    # required for tools
    APPS_LIST+=(
        "btop"
        "ShellCheck"
        #"tealdeer"  -- via repo utilities
        "ripgrep"
        #"pdfgrep" -- onbekend
        "yt-dlp"
        "syncthing"
        "barrier"
        "zip"
        "eza"
        "gdu"
        "xkill"
        "cockpit"
    )

    # required for krusader
    APPS_LIST+=(
        "kget"
        "kompare"
        "krename"
        "7zip"
        "lha"
        "unrar"
        "unzip"
        "zip"
    )
